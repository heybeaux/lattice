import { describe, it, expect } from 'vitest';

describe('@heybeaux/lattice-adapter-mastra', () => {
  it('package exists', () => {
    // Placeholder test — actual tests should be added when Mastra integration is tested locally
    expect(true).toBe(true);
  });
});
